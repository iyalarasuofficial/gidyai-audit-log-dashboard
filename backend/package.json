{
  "name": "audit-log-backend",
  "version": "1.0.0",
  "main": "server.js",
  "description": "Security audit log dashboard backend",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "generate-logs": "node generate-logs.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["audit", "logs", "security"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
